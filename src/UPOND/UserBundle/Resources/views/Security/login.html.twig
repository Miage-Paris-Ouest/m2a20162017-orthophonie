{# src/OC/UserBundle/Resources/views/Security/login.html.twig #}

{% extends "UPONDOrthophonieBundle::layout.html.twig" %}

{% block body %}

    {# S'il y a une erreur, on l'affiche dans un joli cadre #}
    {% if error %}
        <div class="alert alert-danger">{{ error.message }}</div>
    {% endif %}

    {# Le formulaire, avec URL de soumission vers la route « login_check » comme on l'a vu #}
    <form action="{{ path('fos_user_security_check') }}" method="post">
        <div class="form-group">
            <label for="username">Login :</label>
            <input type="text" id="username" name="_username" class='form-control' style='width: 250px' value="{{ last_username }}" />
        </div>

        <div class="form-group">
            <label for="password">Mot de passe :</label>
            <input type="password" id="password" name="_password" class='form-control' style='width: 250px'/>
        </div>

        <input type="submit" value="Connexion" class="btn btn-primary" />
    </form>

{% endblock %}