language: php

php:
 - '5.6'
 - '7.0'
 - '7.1'

before_install:
 -mysql

before_script:
  - curl -s http://getcomposer.org/installer | php --
  - php composer.phar install

before_install:
  - mysql -e 'CREATE DATABASE IF NOT EXISTS orthophonie;'  
  - mysql -u root --default-character-set=utf8 orthophonie < orthophonie.sql
  
  env:
  global:
    - "FTP_USER=root"
    - "FTP_PASSWORD=root"
after_success:
    "curl --ftp-create-dirs -T uploadfilename -u $FTP_USER:$FTP_PASSWORD ftp://176.159.0.42/home/oracle/myfile"
  
